When code changes are complete, run at minimum the relevant Go validators: `go test ./...`, `go vet ./...`, and build or targeted race/integration tests when relevant. For broader verification, use Makefile targets (`make test`, `make vet`, `make build`, `make race`, `make integration`) based on the change scope. For diagnostics/hook work, also verify runtime behavior of edit-time and read-time diagnostics paths without auto-fixing files.