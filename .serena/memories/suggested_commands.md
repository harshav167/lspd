Development commands: `go build ./...`, `go test ./...`, `go test -race ./...`, `go vet ./...`, `go test -race -tags integration ./test/integration/...`. Makefile targets: `make build`, `make test`, `make race`, `make vet`, `make integration`. Useful Darwin shell utilities available: `git`, `ls`, `rg`, `curl`, `gh`, `python3` (but prefer project-specific tooling), standard POSIX tools.