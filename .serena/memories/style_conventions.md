Project uses Go 1.26.x with internal package structure and conventional lower_snake_case file names. Prefer existing package boundaries under internal/. Follow existing Go style: package-scoped types/functions, standard formatting via gofmt/go toolchain, minimal comments unless necessary. Configuration files use YAML (`droid-lsp-config.yaml`) and JSON (`droid-lsp-settings.json`) with hook wiring.