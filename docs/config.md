# Configuration

The daemon reads `lspd.yaml` from the current project tree or `~/.config/lspd/config.yaml`.

Important keys:

- `mcp.host`
- `socket.path`
- `policy.max_per_file`
- `policy.max_per_turn`
- `languages`
